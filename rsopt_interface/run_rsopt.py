from __future__ import annotations

from pathlib import Path
import subprocess

def run_rsopt(rsopt_simulation_files_path: Path, results_path: Path):
    # for now just set status to Success
    subprocess
    (results_path / 'status.txt').write_text('success')
    
